//problem: Write a program to compare two numbers using `>` and `<` and log the result to the console.

const number1 = 56;
const number2 = 45;
console.log(number1 > number2); //true
console.log(number1 < number2); //false

//problem: Write a program to compare two numbers using `>=` and `<=` and log the result to the console.

//problem:  Write a program to compare two numbers using `==` and `===` and log the result to the console. 
const num1 = 25;
const num2 = '25';
console.log(num1 == num2); // true
console.log(num1 === num2); // false