//problem: Write a program that uses the `&&` operator to combine two conditions and log the result to the console. 
const myResult = 88;
if(myResult >=80 && myResult < 90){
    console.log('I have gotten "A+"');
}


// problem: Write a program that uses the `||` operator to combine two conditions and log the result to the console.


//problem: Write a program that uses the `!` operator to negate a condition and log the result to the console. 