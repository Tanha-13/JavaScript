// problem: Use a `for` loop to iterate over the array and log each element to the console.
const arr = [1,2,3,4,5];
for(let i = 0; i< arr.length; i++){
    console.log(arr[i]);
}

// problem: Use the `forEach` method to iterate over the array and log each element to the console.
const users = ['John', 'Doe', 'Natasha', 'De', 'Alex'];
users.forEach(user => console.log(user));